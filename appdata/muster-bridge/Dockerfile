# Real Dockerfile, matching the rest of Citadel's lightweight-Python-
# service pattern (see e.g. appdata/project-vigil/Dockerfile.vigil's
# own comment) -- pure stdlib (socket/http.server/hmac/json), no
# runtime pip install needed at all, so no requirements file either.
FROM python:3.10-alpine
WORKDIR /app
