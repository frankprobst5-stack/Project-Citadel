# Trimmed from the upstream Cloud9 repo's requirements.txt for this
# Docker deployment: drops `pyinstaller`, which only exists there to
# build the Windows desktop installer (see BUILD_INSTRUCTIONS.md) and
# has no purpose running the Flask service itself in a container.
# llama-cpp-python removed 2026-09-14 -- ai.py now calls Citadel's
# shared Ollama over HTTP (via `requests`, already listed below) instead
# of loading its own private model, so there's no native code to compile
# here anymore either (see Dockerfile's own comment).
Flask==3.0.3
requests
yt-dlp
nltk
# Cross-Subject Interactivity Engine's live event bus (see
# interactivity.py) -- SQLite is the durable source of truth, this is
# only the best-effort broadcast layer, so a missing/unreachable Redis
# never breaks a card.
redis==5.0.8
# Weather Data Engine's real GRIB2 decode pipeline (see
# weather_data_engine.py / CARD_REDESIGN_PLAN.md) -- eccodes' compiled
# backend ships bundled in its own PyPI wheel, verified directly, so no
# extra apt package (e.g. libeccodes-dev) is needed in the Dockerfile.
cfgrib==0.9.15.1
xarray==2026.7.0
